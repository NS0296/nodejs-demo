<%- include("../../inc/head.ejs") %>
    </head>
    <body>
        <header>
            <%- include("../inc/nav.ejs") %>
        </header>
        <main>
            <h1>Dashboard</h1>
            <div class="profile">
                <h2>Profile:</h2>
                <dl>
                    <p>
                        <dt>Username:</dt>
                        <dd><%= username %></dd>
                    </p>
                    <p>
                        <dt>Email:</dt>
                        <dd><%= email %></dd>
                    </p>
                    <p>
                        <dt>Phone:</dt>
                        <dd><%= phone %> </dd>
                    </p>
                    <p>
                        <dt>Address:</dt>
                        <dd><%= address %> </dd>
                    </p>
                </dl>
            </div>
            <form action="/logout" method="GET">
                <button type="submit">Logout</button>
            </form>
        </main>
<%- include("../inc/end.ejs") %>